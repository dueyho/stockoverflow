<%= form_for @answer, remote: true do |f| %>

  <%= f.hidden_field(:question_id, :value => @question.id) %>
  <%= f.label :answer_text, "Answer This Question" %>
  <%= f.text_field :answer_text %>
  <%= f.submit(:class => 'btn btn-success') %>
<% end %>

